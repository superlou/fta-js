import treeNode from 'fta-js/utils/tree-node';
import { module, test } from 'qunit';

module('Unit | Utility | TreeNode', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = treeNode();
    assert.ok(result);
  });
});
